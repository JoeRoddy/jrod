{
  "name": "jrod",
  "version": "1.0.9",
  "description": "Misc development scripts",
  "type": "commonjs",
  "bin": {
    "jrod": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "chmod +x dist/cli.js mknext.sh",
    "prepare": "npm run build && npm run postbuild",
    "npm-publish": "npm run prepare && npm version patch && npm publish",
    "start": "node dist/cli.js --help"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/node": "^20.4.2"
  }
}
