{
  "name": "jrod",
  "version": "1.0.10",
  "description": "Misc development scripts",
  "type": "commonjs",
  "bin": {
    "jrod": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build && npm run postbuild",
    "npm-publish": "npm run prepare && npm version patch && npm publish",
    "start": "node dist/cli.js --help",
    "mknext:test": "JR_SCRIPTS_FORCE_DEV=1 npx tsx ./src/cli.ts mknext test-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.1.0",
    "eta": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "typescript": "^5.2.2"
  }
}
